// Top-level build file where you can add configuration options common to all sub-projects/modules.

apply plugin: "com.github.ben-manes.versions" /* Project Dependency Update Crawler Plugin */

buildscript {

    ext {
        globalDependencyVersions = [
            gradleVersion : '4.2.0-alpha12', // Gradle Configuration
            kotlinVersion : '1.3.61', // Kotlin Configuration
            seekVersions  : '0.28.0' // Project Dependency Update Crawler Plugin
        ]
    }

    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath "com.android.tools.build:gradle:" \
            + "$globalDependencyVersions.gradleVersion"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:" \
            + "$globalDependencyVersions.kotlinVersion"
        classpath "com.github.ben-manes:gradle-versions-plugin:" \
            + "$globalDependencyVersions.seekVersions"
    }

}

allprojects {

    repositories {
        google()
        jcenter()
    }

}

task clean(type: Delete) {
    delete rootProject.buildDir
}
