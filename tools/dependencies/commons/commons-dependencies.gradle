apply from: "$project.rootDir/tools/dependencies/commons/commons-libraries.gradle"

dependencies { configuration ->
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    // add more dependency groups here
}
